meta {
  name: Confirm Check In
  type: http
}

post {
  url: {{baseUrl}}/v1/tickets/{{ticketId}}/scanner-check-in
  body: json
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

body:json {
  {
    "gateId": "00000000-0000-4000-8000-000000000000",
    "reason": "Test"
  }
}
